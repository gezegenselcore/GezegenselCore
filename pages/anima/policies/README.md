# Legacy Anima policy paths (redirects only)

This folder does **not** host legal text. The only public Anima legal URLs are:

- `/anima/privacy-policy.html`
- `/anima/terms-of-use.html`
- `/pages/anima/support.html`

The HTML files here are minimal **redirect stubs** (meta refresh + manual link) for old bookmarks and external links.
